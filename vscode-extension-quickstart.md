# Top-Down (VS Code Extension)

## Run locally

1. `npm install`
2. Press `F5` to launch an Extension Development Host.
3. Open the bottom panel tab: **Top-Down → Config**.

## Source of truth

The panel table is the authoritative store at `.topdown/config.json`.
